<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SelfCheckoutKiosk.App.Views.Customer.PaymentOptionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SelfCheckoutKiosk.App.Views.Customer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- ENABLED CARD STYLE -->
        <Style x:Key="PaymentCardEnabledStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        </Style>

        <!-- DISABLED CARD STYLE -->
        <Style x:Key="PaymentCardDisabledStyle" TargetType="Button">
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Opacity" Value="0.6"/>
        </Style>

        <Style x:Key="PaymentIconStyle" TargetType="FontIcon">
            <Setter Property="FontFamily" Value="Segoe Fluent Icons"/>
            <Setter Property="FontSize" Value="110"/>
        </Style>

        <Style x:Key="PaymentTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="34"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,20,0,6"/>
        </Style>

        <Style x:Key="PaymentSubtitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
    </Page.Resources>

    <Grid Background="White">
        <!-- ========================================================= -->
        <!-- VISUAL STATE MANAGER (ADAPTIVE RESPONSIVE STATES)          -->
        <!-- ========================================================= -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="DisplayStates">
                <!-- DEFAULT STATE: Optimized for Small Vertical Testing Displays (< 1200px Height) -->
                <VisualState x:Name="SmallVerticalState" />

                <!-- LARGE VERTICAL KIOSK STATE: 27.5 Inch (1080x1920) -->
                <VisualState x:Name="LargeVerticalKioskState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowHeight="1200" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="HeaderContainer.Margin" Value="64,48,64,36" />
                        <Setter Target="CardsGrid.Padding" Value="64,40" />
                        <Setter Target="FooterContainer.Padding" Value="64,0,64,48" />
                        <Setter Target="HeaderTitle.FontSize" Value="48" />
                        <Setter Target="HeaderSubTitle.FontSize" Value="20" />
                        <Setter Target="BackButton.Height" Value="96" />
                        <Setter Target="BackButtonText.FontSize" Value="28" />
                    </VisualState.Setters>
                </VisualState>

                <!-- HORIZONTAL KIOSK STATE: 17.3 Inch (1920x1080) -->
                <VisualState x:Name="HorizontalKioskState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1200" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="HeaderContainer.Margin" Value="48,16,48,12" />
                        <Setter Target="CardsGrid.Padding" Value="48,16" />
                        <Setter Target="FooterContainer.Padding" Value="48,0,48,20" />
                        <Setter Target="HeaderTitle.FontSize" Value="32" />
                        <Setter Target="HeaderSubTitle.FontSize" Value="14" />
                        <Setter Target="BackButton.Height" Value="64" />
                        <Setter Target="BackButtonText.FontSize" Value="20" />
                        <!-- Switch Cards to 3 Columns x 2 Rows for Horizontal Display -->
                        <Setter Target="CardsGrid.ColumnDefinitions[0].Width" Value="*" />
                        <Setter Target="CardsGrid.ColumnDefinitions[1].Width" Value="20" />
                        <Setter Target="CardsGrid.ColumnDefinitions[2].Width" Value="*" />
                        <Setter Target="CardsGrid.ColumnDefinitions[3].Width" Value="20" />
                        <Setter Target="CardsGrid.ColumnDefinitions[4].Width" Value="*" />
                        <Setter Target="CardsGrid.RowDefinitions[0].Height" Value="*" />
                        <Setter Target="CardsGrid.RowDefinitions[1].Height" Value="20" />
                        <Setter Target="CardsGrid.RowDefinitions[2].Height" Value="*" />
                        <Setter Target="CardsGrid.RowDefinitions[3].Height" Value="0" />
                        <Setter Target="CardsGrid.RowDefinitions[4].Height" Value="0" />
                        <!-- Reposition Cards for 3x2 Grid Placement -->
                        <Setter Target="CardViewbox.Grid.Row" Value="0" />
                        <Setter Target="CardViewbox.Grid.Column" Value="4" />
                        <Setter Target="IntlQrViewbox.Grid.Row" Value="2" />
                        <Setter Target="IntlQrViewbox.Grid.Column" Value="0" />
                        <Setter Target="MembershipViewbox.Grid.Row" Value="2" />
                        <Setter Target="MembershipViewbox.Grid.Column" Value="2" />
                        <Setter Target="CouponViewbox.Grid.Row" Value="2" />
                        <Setter Target="CouponViewbox.Grid.Column" Value="4" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <!-- 0: Header -->
            <RowDefinition Height="Auto"/>
            <!-- 1: Payment cards grid -->
            <RowDefinition Height="*"/>
            <!-- 2: Back button -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ========================================================= -->
        <!-- 0. HEADER                                                 -->
        <!-- ========================================================= -->
        <Grid Grid.Row="0" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1">
            <Viewbox x:Name="HeaderViewbox" Stretch="Uniform" StretchDirection="DownOnly" HorizontalAlignment="Left" x:FieldModifier="public">
                <Grid x:Name="HeaderContainer" Margin="32,20,32,16">
                    <StackPanel Spacing="4" HorizontalAlignment="Left">
                        <TextBlock x:Name="HeaderSubTitle" Text="OPTION" FontSize="14" FontWeight="SemiBold" Foreground="#94A3B8" CharacterSpacing="100"/>
                        <TextBlock x:Name="HeaderTitle" Text="Payment Method" FontSize="36" FontWeight="Bold" Foreground="#1A202C"/>
                    </StackPanel>
                </Grid>
            </Viewbox>
        </Grid>

        <!-- ========================================================= -->
        <!-- 1. PAYMENT CARDS                                          -->
        <!-- Dynamic Layout: 2x3 (Vertical) or 3x2 (Horizontal)         -->
        <!-- ========================================================= -->
        <Grid x:Name="CardsGrid" Grid.Row="1" Padding="32,20" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="0"/>
                <ColumnDefinition Width="0"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ===== CASH CARD ===== -->
            <Viewbox x:Name="CashViewbox" Grid.Row="0" Grid.Column="0" Stretch="Uniform" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Button x:Name="CashCard" Width="360" Height="360" Style="{StaticResource PaymentCardEnabledStyle}">
                    <Button.Resources>
                        <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#F8FAFC"/>
                        <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#F1F5F9"/>
                        <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="#2563EB"/>
                        <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="#2563EB"/>
                    </Button.Resources>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE8C7;" Foreground="#2563EB" Style="{StaticResource PaymentIconStyle}"/>
                        <TextBlock Text="Cash" Foreground="#1E293B" Style="{StaticResource PaymentTitleStyle}"/>
                        <TextBlock Text="USD / KHR" Foreground="#64748B" Style="{StaticResource PaymentSubtitleStyle}"/>
                    </StackPanel>
                </Button>
            </Viewbox>

            <!-- ===== KHQR CARD ===== -->
            <Viewbox x:Name="KhqrViewbox" Grid.Row="0" Grid.Column="2" Stretch="Uniform" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Button x:Name="KhqrCard" Width="360" Height="360" Style="{StaticResource PaymentCardEnabledStyle}">
                    <Button.Resources>
                        <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#F8FAFC"/>
                        <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#F1F5F9"/>
                        <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="#2563EB"/>
                        <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="#2563EB"/>
                    </Button.Resources>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xED14;" Foreground="#2563EB" Style="{StaticResource PaymentIconStyle}"/>
                        <TextBlock Text="KHQR" Foreground="#1E293B" Style="{StaticResource PaymentTitleStyle}"/>
                        <TextBlock Text="Digital" Foreground="#64748B" Style="{StaticResource PaymentSubtitleStyle}"/>
                    </StackPanel>
                </Button>
            </Viewbox>

            <!-- ===== DEBIT / CREDIT CARD ===== -->
            <Viewbox x:Name="CardViewbox" Grid.Row="2" Grid.Column="0" Stretch="Uniform" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Button x:Name="CardCard" Width="360" Height="360" Style="{StaticResource PaymentCardEnabledStyle}">
                    <Button.Resources>
                        <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#F8FAFC"/>
                        <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#F1F5F9"/>
                        <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="#2563EB"/>
                        <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="#2563EB"/>
                    </Button.Resources>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE8C7;" Foreground="#2563EB" Style="{StaticResource PaymentIconStyle}"/>
                        <TextBlock Text="Debit / Credit Card" Foreground="#1E293B" Style="{StaticResource PaymentTitleStyle}"/>
                    </StackPanel>
                </Button>
            </Viewbox>

            <!-- ===== INTERNATIONAL QR CARD ===== -->
            <Viewbox x:Name="IntlQrViewbox" Grid.Row="2" Grid.Column="2" Stretch="Uniform" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Button x:Name="IntlQrCard" Width="360" Height="360" Style="{StaticResource PaymentCardEnabledStyle}">
                    <Button.Resources>
                        <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#F8FAFC"/>
                        <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#F1F5F9"/>
                        <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="#2563EB"/>
                        <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="#2563EB"/>
                    </Button.Resources>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xED14;" Foreground="#2563EB" Style="{StaticResource PaymentIconStyle}"/>
                        <TextBlock Text="International QR" Foreground="#1E293B" Style="{StaticResource PaymentTitleStyle}"/>
                    </StackPanel>
                </Button>
            </Viewbox>

            <!-- ===== MEMBERSHIP CARD ===== -->
            <Viewbox x:Name="MembershipViewbox" Grid.Row="4" Grid.Column="0" Stretch="Uniform" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Button x:Name="MembershipCard" Width="360" Height="360" Style="{StaticResource PaymentCardEnabledStyle}">
                    <Button.Resources>
                        <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#F8FAFC"/>
                        <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#F1F5F9"/>
                        <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="#2563EB"/>
                        <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="#2563EB"/>
                    </Button.Resources>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE8C3;" Foreground="#2563EB" Style="{StaticResource PaymentIconStyle}"/>
                        <TextBlock Text="Membership Card" Foreground="#1E293B" Style="{StaticResource PaymentTitleStyle}"/>
                    </StackPanel>
                </Button>
            </Viewbox>

            <!-- ===== COUPON CARD ===== -->
            <Viewbox x:Name="CouponViewbox" Grid.Row="4" Grid.Column="2" Stretch="Uniform" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Button x:Name="CouponCard" Width="360" Height="360" Style="{StaticResource PaymentCardEnabledStyle}">
                    <Button.Resources>
                        <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#F8FAFC"/>
                        <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#F1F5F9"/>
                        <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="#2563EB"/>
                        <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="#2563EB"/>
                    </Button.Resources>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE8EC;" Foreground="#2563EB" Style="{StaticResource PaymentIconStyle}"/>
                        <TextBlock Text="Coupon" Foreground="#1E293B" Style="{StaticResource PaymentTitleStyle}"/>
                    </StackPanel>
                </Button>
            </Viewbox>
        </Grid>

        <!-- ========================================================= -->
        <!-- 2. BACK BUTTON                                            -->
        <!-- ========================================================= -->
        <Grid x:Name="FooterContainer" Grid.Row="2" Padding="32,0,32,24">
            <Button x:Name="BackButton" Height="72" HorizontalAlignment="Stretch" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Background="White"
                    BorderBrush="#CBD5E1" BorderThickness="2"  CornerRadius="16" Click="BackButton_Click">
                <Button.Resources>
                    <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#F8FAFC"/>
                    <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#F1F5F9"/>
                    <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="#94A3B8"/>
                    <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="#94A3B8"/>
                </Button.Resources>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
                    <TextBlock x:Name="BackButtonText" Text="BACK" FontSize="22" FontWeight="Bold" Foreground="#334155" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</Page>