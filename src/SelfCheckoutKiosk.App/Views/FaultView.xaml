<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SelfCheckoutKiosk.App.Views.FaultView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SelfCheckoutKiosk.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--
        Blocking failure screen shown when LLCoreLogicEngine.InitializeAsync()
        fails (license validation, hardware connect, etc). Blueprint §6 step 2:
        treat failure as a hard stop, not a degraded mode — this page is the
        only thing reachable when that happens; nothing here navigates onward
        to the Cart/Home/Payment flow.
    -->
    <Grid Background="#1A1A1A">
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            MaxWidth="560"
            Spacing="20">

            <TextBlock
                Text="Kiosk engine failed to start"
                Foreground="#FF6B6B"
                FontSize="28"
                FontWeight="Bold"
                TextWrapping="Wrap"
                HorizontalAlignment="Center" />

            <TextBlock
                x:Name="ReasonTextBlock"
                Foreground="White"
                FontSize="16"
                TextWrapping="Wrap"
                HorizontalAlignment="Center"
                TextAlignment="Center" />

            <TextBlock
                Text="Contact support. Do not attempt to process transactions on this kiosk."
                Foreground="#BBBBBB"
                FontSize="14"
                TextWrapping="Wrap"
                HorizontalAlignment="Center"
                TextAlignment="Center"
                Margin="0,20,0,0" />
        </StackPanel>
    </Grid>
</Page>
